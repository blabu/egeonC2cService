import React, {Fragment} from 'react'
import {Typography} from '@material-ui/core'

export default function Quit(props) {
    return (
        <Fragment>
            <Typography variant="h6">Quit page</Typography>
        </Fragment>
    )
}